---
import { link } from '@astro/types';

const title = 'Create New Match';

export default function NewMatch() {
  // Implement logic to handle form submission and API request here

  return (
    <main>
      <h1>{title}</h1>
      <form on:submit|preventDefault={handleSubmit}>
        <!-- Add form fields for match details -->
        <button type="submit">Create Match</button>
      </form>
      <button on:click={() => link('/')} class="back-button">Back to Home</button>
    </main>
  );

  function handleSubmit() {
    // Handle form submission and API request to create a new match
  }
}
