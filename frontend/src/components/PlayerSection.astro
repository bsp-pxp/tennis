---
const sectionTitle = "Player 1";
function handlePoint(pointType: string) {
    console.log(`Point recorded: ${pointType}`);
}
---

<script>
  function handlePoint(pointType: string) {
    console.log(`Point recorded: ${pointType}`);
}
</script>

<script>
  console.log('Welcome, browser console!');
</script>

<!-- Player and Opponent sections -->
<section class="player">
  <h2>{sectionTitle}</h2>
  <div class="options">
    <div class="column">
      <button on:click={() => handlePoint("winner")}>Winner (Forehand)</button>
      <button on:click={() => handlePoint("error")}>Error (Forehand)</button>
    </div>
    <div class="column">
      <button on:click={() => handlePoint("winner")}>Winner (Forehand)</button>
      <button on:click={() => handlePoint("error")}>Error (Forehand)</button>
    </div>
  </div>
</section>
